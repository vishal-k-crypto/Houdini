# Core
rich>=13.0.0
pyautogui>=0.9.54
pytesseract>=0.3.10
pillow>=10.0.0
llama-cpp-python>=0.2.90
playwright>=1.48.0
python-dotenv>=1.0.0
google-genai>=0.2.0

# Data Validation
pydantic>=2.0.0

# Vector Database for Lesson Store (RAG-based learning)
# Using FAISS for efficient similarity search (ChromaDB has dependency conflicts)
faiss-cpu>=1.7.0

# NOTE: Ollama is required but installed separately (not a Python package)
# Install from: https://ollama.ai or run: brew install ollama

# macOS Accessibility API (native integration)
pyobjc-core>=10.0
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-Quartz>=10.0
pyobjc-framework-ApplicationServices>=10.0

# Human-like cursor control
numpy>=1.24.0
scipy>=1.11.0

# Probability Model for flexible task execution (NEW)
# These enable intelligent handling of incomplete/ambiguous task specs
scikit-fuzzy>=0.5.0    # Fuzzy logic for macro-micro spectrum
pgmpy>=1.0.0           # Bayesian networks for probabilistic reasoning
networkx>=3.0          # Required by pgmpy

# Execution Confidence Model (NEW)
# Enables confidence-based action execution with calibration and learning
# Uses built-in algorithms inspired by:
# - uncertainty-toolbox: calibration metrics
# - uncertainty-calibration: Platt scaling, isotonic regression
# - mabwiser: Thompson Sampling for exploration/exploitation
# No additional packages needed - uses numpy which is already included

# LangGraph for state management and cyclic agent execution
# Replaces custom loop coordinators with declarative graph-based execution
# Benefits: checkpointing, human-in-the-loop, crash recovery, visualization
langgraph>=0.2.0
langchain-core>=0.3.0

# Textual TUI Framework (replaces Tkinter for thinking window)
# Beautiful terminal UI with hacker aesthetic
# Provides: rich text, interactive widgets, CSS-like styling
textual>=0.45.0

# ============================================================
# LOCAL VISION LOCALIZER - Hybrid Apple Vision + UI-TARS MLX
# ============================================================
# Precise pixel localization using native macOS frameworks + local MLX models
# Benefits:
# - Hardware-accelerated on Apple Silicon Neural Engine
# - Sub-millisecond detection for standard UI shapes
# - No cloud latency - runs entirely on-device
# - Semantic precision for niche tools (Adobe, Electron apps)

# Apple Vision Framework (included in pyobjc packages above)
# Already have: pyobjc-framework-Quartz, pyobjc-framework-Cocoa
# Add Vision framework support:
pyobjc-framework-Vision>=10.0

# MLX-VLM for UI-TARS (Apple Silicon optimized) - OPTIONAL
# Semantic grounding for complex/non-accessible UI elements
# To enable: pip install mlx-vlm
# This will install MLX and optimized vision-language models
# Recommended model: mlx-community/UI-TARS-7B-SFT-4bit (~4GB)
# mlx-vlm>=0.1.0
